class Main {
    function void main()
    {
        // do Screen.setColor(true);
        // do Screen.drawRectangle(25, 25, 100, 100);

        var RunLoop runLoop; 
        let runLoop = RunLoop.new();
        do runLoop.start();

        return;
    }
}

